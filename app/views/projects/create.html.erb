<%= link_to 'Volver al Inicio', root_path %>
<% if @project.errors.any? %>
    <div id="error_explanation">
        <h2>No se puede guardar el post por las siguientes razones:</h2>
        <ul><% @project.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
        </ul>
    </div>
    <% else %>
    <h1>¡Registro exitoso!</h1>
<% end  %>
